# Private companion repo: its own git repository, never part of this one.
private/
# Retired writing clone; keep any local copy out of the public repo.
/writing/

# Data and pipeline outputs live beside the code, outside version control.
/Data/
/Results/

# Local git worktrees.
/.wt/

# Agent guidelines are local only, at any depth.
AGENTS.md
CLAUDE.md

.env
.DS_Store

# R outputs
*.log
Rplots.pdf
code/DataIntermediate/freq_obs_Annual.csv
code/DataIntermediate/validDenomsCombinations.csv

# LaTeX outputs
*.aux
*.bbl
*.bbl*
*.bcf
*.blg
*.fls
*.fdb_latexmk
*.idx
*.ilg
*.ind
*.lof
*.lot
*.out
*.run.xml
*.toc
*.synctex*
*.xdv
*.dvi
# LaTeX beamer aux
*.nav
*.snm
*.vrb
# Class and style files are fetched per build, except the paper's own style.
*.sty
*.cls
*.bst
!paper/zimstyle.sty

# Built paper; exhibits stay tracked so a clone builds without the pipeline.
/paper/*.pdf

# LaTeX preview build artifact
code/LatexPreview/exhibits.pdf

# Slide decks: internal, kept locally only.
# These are responses to a named discussant and this remote is public.
# Removed from history in 2026-07; do not re-add.
slides_*.tex
slides_*.pdf

# Scratch directories (code/dev/, paper/dev/)
dev/
temp/
scratch.md

# Claude Code local settings
.claude/settings.local.json
